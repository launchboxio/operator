---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: servicecatalogs.core.launchboxhq.io
spec:
  group: core.launchboxhq.io
  names:
    kind: ServiceCatalog
    listKind: ServiceCatalogList
    plural: servicecatalogs
    singular: servicecatalog
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServiceCatalog is the Schema for the servicecatalogs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ServiceCatalogSpec defines the desired state of ServiceCatalog
            properties:
              services:
                items:
                  properties:
                    git:
                      properties:
                        auth:
                          description: Auth defines the authentication method for
                            git
                          properties:
                            token:
                              description: Token Specify a token for cloning git repos.
                                This can be configured globally at the space level,
                                or for private installs, at the operator level
                              type: string
                          required:
                          - token
                          type: object
                        host:
                          description: Host defines the hostname of the git provider.
                            Defaults to "github.com?
                          type: string
                        repository:
                          description: Repository is the "${owner}/${project}" name
                            for a repo
                          type: string
                      required:
                      - repository
                      type: object
                    helm:
                      properties:
                        path:
                          type: string
                      required:
                      - path
                      type: object
                    raw:
                      properties:
                        raw:
                          type: string
                      required:
                      - raw
                      type: object
                  type: object
                type: array
            type: object
          status:
            description: ServiceCatalogStatus defines the observed state of ServiceCatalog
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
