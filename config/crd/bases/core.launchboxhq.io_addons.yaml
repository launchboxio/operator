---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: addons.core.launchboxhq.io
spec:
  group: core.launchboxhq.io
  names:
    kind: Addon
    listKind: AddonList
    plural: addons
    singular: addon
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Addon is the Schema for the addons API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AddonSpec defines the desired state of Addon
            properties:
              git:
                properties:
                  auth:
                    description: Auth defines the authentication method for git
                    properties:
                      token:
                        description: Token Specify a token for cloning git repos.
                          This can be configured globally at the space level, or for
                          private installs, at the operator level
                        type: string
                    required:
                    - token
                    type: object
                  host:
                    description: Host defines the hostname of the git provider. Defaults
                      to "github.com?
                    type: string
                  repository:
                    description: Repository is the "${owner}/${project}" name for
                      a repo
                    type: string
                required:
                - repository
                type: object
              helm:
                properties:
                  chart:
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                  repo:
                    type: string
                  values:
                    type: string
                  version:
                    type: string
                required:
                - chart
                - namespace
                - repo
                type: object
              outputs:
                additionalProperties:
                  type: string
                type: object
              provider:
                description: 'Supported providers: git - clone git, and apply raw
                  manifests git+helm - clone git, and apply helm chart at path helm
                  - install helm chart from registry git+terraform - clone git, and
                  install terraform at path raw - apply raw manifests inline git+kustomize
                  - clone git repo, and apply kustomize url - apply manifests from
                  a remote URL shell - escape hatch; run arbitrary commands. Note
                  this is only available on self-hosted installations'
                type: string
              raw:
                items:
                  type: string
                type: array
              space:
                type: string
              terraform:
                type: object
            required:
            - provider
            - space
            type: object
          status:
            description: AddonStatus defines the observed state of Addon
            properties:
              lastError:
                type: string
              state:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
